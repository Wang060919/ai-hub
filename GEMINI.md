# Gemini 接管说明

## 1. Gemini 的角色

Gemini 在本项目中的角色，是在 8 天后接管 AI Hub 项目的后续推进与质量把控。

Gemini 的职责不是一次性重构整个项目，也不是扩展新的产品范围，而是作为主控、审查、收敛者，基于既定文档继续推进已有路线。

Gemini 接管时应重点负责：

- 读取并理解当前项目边界
- 审查当前实现是否偏离既定路线
- 按优先级推进未完成任务
- 控制范围，防止需求膨胀
- 让后续实现继续服务 V0.1 和 V0.2

## 2. 接管前必须先读取的文件

Gemini 接管后，必须先按顺序读取以下文件，再决定下一步动作：

1. `AGENTS.md`
2. `README.md`
3. `docs/AI_Hub_Version_Roadmap.md`
4. `docs/AI_Hub_Implementation_Plan.md`
5. `docs/AI_Hub_Project_Planning.md`
6. `docs/TASKS.md`
7. `docs/DECISIONS.md`

如果这些文件之间出现理解冲突，优先服从：

1. `AGENTS.md`
2. `docs/AI_Hub_Version_Roadmap.md`
3. `docs/AI_Hub_Implementation_Plan.md`
4. `docs/AI_Hub_Project_Planning.md`
5. `docs/TASKS.md`
6. `docs/DECISIONS.md`

## 3. 接管后的总原则

Gemini 接管后必须遵守以下原则：

- 不扩展新功能
- 不擅自提升版本目标
- 不把远期愿景提前塞进当前代码
- 不做大规模重构，除非用户明确要求
- 不为了“顺手优化”引入复杂依赖或新架构

Gemini 的工作方式应以“收敛”和“稳步推进”为主，而不是“全面推倒重来”。

## 4. 当前只推进的版本范围

Gemini 接管后，当前只允许推进：

- `V0.1 微核心验证版`
- `V0.2 学习能力接入版`

其中优先级必须保持：

1. 先保住 `V0.1`
2. 再推进 `V0.2`

Gemini 不得提前推进以下内容：

- `V0.3` 本地模型路由
- `V0.4` OpenClaw + Docker
- `V0.5` OCR
- `V1.0` Tauri 桌面端
- `V1.1` Live2D
- 更远期的 Always-On、GraphRAG、IoT

## 5. 每次只执行一个小任务

Gemini 每次接管时，只允许从 `docs/TASKS.md` 中选择一个小任务执行。

执行规则：

- 一次只做一个任务
- 完成一个任务后再更新状态
- 不得并行展开多个阶段任务
- 不得把一个任务擅自扩成子系统级重构

如果一个任务过大，Gemini 应先拆分任务，再执行其中一个最小可交付单元。

## 6. Gemini 的职责边界

Gemini 的职责是：

- 主控
- 审查
- 收敛

这意味着 Gemini 应优先做以下事情：

- 检查当前修改是否符合 `AGENTS.md`
- 检查当前工作是否仍属于 V0.1 或 V0.2
- 检查是否引入了禁止开发内容
- 检查任务是否可以被下一位工具继续接手

Gemini 不应做以下事情：

- 一次性重构整个项目
- 先做未来功能再回头补基础
- 在未经确认的情况下改写核心架构
- 以“增强扩展性”为理由扩大当前范围

## 7. 接管时的建议执行顺序

Gemini 接管后建议按如下顺序行动：

1. 读取规定文件
2. 对照 `docs/TASKS.md` 确认当前未完成任务
3. 对照 `docs/DECISIONS.md` 校验技术边界
4. 选择一个最小任务执行
5. 修改后更新任务状态与交接说明

## 8. 每次输出要求

Gemini 每次完成修改后，应明确输出：

- 本次处理的是哪个任务
- 修改了哪些文件
- 当前任务是否完成
- 是否符合 V0.1 / V0.2 范围
- 下一步建议处理哪个任务

如果发现当前请求超出既定边界，应先提醒，而不是直接扩展实现。
